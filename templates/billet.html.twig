{% extends "base.html.twig" %}
{% block titre %}
    {{ article.id }}
{% endblock %}
{% block section %}
    <main>
        <img src="{{ article.image }}" alt="{{ article.image }}" >
        <h3>">{{ article.title }}</h3>
        <p>{{ article.content }}</p>
    </main>

{% endblock %}